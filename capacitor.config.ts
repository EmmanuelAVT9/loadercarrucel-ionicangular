import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-loadercarrucel-emmavaztru',
  webDir: 'www'
};

export default config;
